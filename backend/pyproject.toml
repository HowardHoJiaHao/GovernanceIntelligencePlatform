# backend/pyproject.toml
[project]
name = "sya-backend"
version = "0.1.0"
dependencies = [
    "flask>=2.3.0",
    "flask-cors>=4.0.0",
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "werkzeug>=2.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

